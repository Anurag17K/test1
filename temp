- name: Wait for EB application version to be processed
  run: |
    aws elasticbeanstalk wait application-version-processed \
      --application-name ${{ env.EB_APP_NAME }} \
      --version-label github-${{ github.run_number }}
